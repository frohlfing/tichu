/**
 * Ladeanzeige
 *
 * @type {View}
 */
const LoadingView = (() => {

    /**
     * Initialisiert die Ladeanzeige.
     */
    function init() {
    }

    /**
     * Rendert die Ladeanzeige.
     */
    function render() {
    }

    return {
        init,
        render,
    };
})();